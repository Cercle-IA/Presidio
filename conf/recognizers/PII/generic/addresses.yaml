# Recognizer pour adresses postales génériques
recognizer_registry:
  recognizers:
    - name: GenericAddressRecognizer
      supported_language: fr
      supported_entity: ADRESSE
      patterns:
        - name: Adresse avec numéro et rue
          regex: "\\b\\d{1,4}(?:bis|ter|quater)?[A-Za-z]?\\s+(?:rue|avenue|boulevard|place|impasse|allée|chemin|route|quai|square|passage|villa|cité|cours|esplanade|promenade|sentier|voie|lotissement)\\s+(?-i:[A-ZÀ-Ÿ])[a-zà-ÿ\\s'-]{3,30}(?=\\s*[-,]|\\s*$|\\s+[0-9]{4,5}|\\s*\\n)"
          score: 0.8
        # Score bas volontaire : un nombre à 4-5 chiffres isolé est
        # indiscernable d'une année ("l'exercice 2024") sans contexte. Ne
        # doit survivre que boosté par un mot du contexte ci-dessous
        # (associé au default_score_threshold défini dans conf/main.yaml).
        - name: Code postal générique
          regex: "\\b(?<!FR[0-9]{2}\\s)(?<![0-9]\\s)[0-9]{4,5}(?!\\s[0-9]{4})(?!\\s[0-9A-Z]{4})\\b"
          score: 0.3
        - name: Adresse complète avec ville et virgule
          regex: "\\b\\d{1,4}(?:bis|ter|quater)?[A-Za-z]?\\s+(?:rue|avenue|boulevard|place|impasse|allée|chemin|route|quai|square|passage|villa|cité|cours|esplanade|promenade|sentier|voie|lotissement)\\s+(?-i:[A-ZÀ-Ÿ])[a-zà-ÿ\\s'-]+?,\\s*[0-9]{4,5}\\s+(?-i:[A-ZÀ-Ÿ])[a-zà-ÿ'-]+(?=\\s*$|\\s*\\n|\\s*-)"
          score: 0.95
        - name: Adresse complète sans virgule
          regex: "\\b\\d{1,4}(?:bis|ter|quater)?[A-Za-z]?\\s+(?:rue|avenue|boulevard|place|impasse|allée|chemin|route|quai|square|passage|villa|cité|cours|esplanade|promenade|sentier|voie|lotissement)\\s+(?-i:[A-ZÀ-Ÿ])[a-zà-ÿ\\s'-]+?\\s+[0-9]{4,5}\\s+(?-i:[A-ZÀ-Ÿ])[a-zà-ÿ'-]+(?=\\s*$|\\s*\\n|\\s*-)"
          score: 0.9
        - name: Adresse simple avec type de voie
          regex: "\\b(?:rue|avenue|boulevard|place|impasse|allée|chemin|route|quai|square|passage|villa|cité|cours|esplanade|promenade|sentier|voie|lotissement)\\s+(?-i:[A-ZÀ-Ÿ])[a-zà-ÿ\\s'-]{3,30}(?=\\s*[-,]|\\s*$|\\s+[0-9]{4,5}|\\s*\\n)"
          score: 0.65
      context:
        ["adresse", "domicile", "résidence", "siège", "demeurant", "résidant"]
